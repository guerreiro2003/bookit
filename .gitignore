.DS_Store
.firebase/
node_modules/
*.log

# Don't commit zips/exports
*.zip

# Editor
.vscode/
.idea/

# Definições locais do Claude Code: caminhos e permissões desta máquina, que
# não valem nada noutra. O settings.json (partilhado) é versionado; este não.
.claude/settings.local.json

# Local Netlify folder
.netlify

# backups / functions deps
backups/
functions/node_modules/

# Segredos. O Hosting publica a raiz do repositório: um .env commitado fica
# público no site, não apenas no histórico.
.env
.env.*
